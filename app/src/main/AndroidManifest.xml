<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.flo.roommateapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />

    <uses-sdk android:minSdkVersion="10" />
<!--
    adb shell am start -a android.intent.action.VIEW -d "http://www.myroommatesapp.com/app" be.flo.roommateapp.vue.activity.vue.activity.LoadingActivity

    be.flo.roommateapp.vue.activity.LoadingActivity

    be.flo.roommateapp/.vue.activity.LoadingActivity

    be.roommate.app/.vue.activity.LoadingActivity

    //adb shell am start -n be.roommate.app/be.flo.roommateapp.vue.activity.LoadingActivity

    adb shell am start -a android.intent.action.VIEW -d "http://www.myroommatesapp.com/app/start" be.flo.roommateapp.vue.activity.vue.activity.LoadingActivity

    adb shell am start -a android.intent.action.VIEW -d "http://www.myroommatesapp.com/app/start?Gc4ER1WYk+5ZsYLGwpBI2Yn2BUn16KlW9cfs+DLhrLhIFCE/HkaSvzvHbQZbuM+O" be.roommate.app


    be.roommate.app://www.myroommatesapp.com/app/start

Gc4ER1WYk+5ZsYLGwpBI2Yn2BUn16KlW9cfs+DLhrLhIFCE/HkaSvzvHbQZbuM+O

    be.roommate.app://www.myroommatesapp.com/app/start
-->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/myTheme">
        <!-- android:theme="@style/myTheme" -->
        <!-- loading activity -->
        <activity
            android:name=".vue.activity.LoadingActivity"
            android:configChanges="orientation|keyboardHidden|keyboard"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!--
                <data android:scheme="http"
                    android:host="www.myroommatesapp.com"
                    android:pathPrefix="/app/start"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="http"
                    android:host="www.myroommatesapp.com"
                    android:pathPrefix="/app/start"/>
                -->
            </intent-filter>
        </activity>

        <!-- main activity -->
        <activity
            android:name=".vue.activity.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard"
            android:label="@string/app_name"
            android:launchMode="singleInstance"></activity>

        <!-- welcome activity -->
        <activity
            android:name=".vue.activity.WelcomeActivity"
            android:label="@string/g_welcome"
            android:launchMode="singleInstance"></activity>

        <!-- registration activity -->
        <activity
            android:name=".vue.activity.RegistrationActivity"
            android:label="@string/g_registration"
            android:launchMode="singleInstance"></activity>

        <!-- login activity -->
        <activity
            android:name=".vue.activity.LoginActivity"
            android:label="@string/g_login"></activity>

        <!-- forgot password activity -->
        <activity
            android:name=".vue.activity.ForgotPasswordActivity"
            android:label="@string/activity_forgot_password"
            android:launchMode="singleInstance"></activity>

        <!-- ROOMMATE -->

        <activity
            android:name=".vue.activity.edit.EditRoommateActivity"
            android:label="@string/roommate_create"
            android:noHistory="true"></activity>

        <!-- TICKET -->
        <activity
            android:name=".vue.activity.edit.EditTicketActivity"
            android:label="@string/createTicket_title"
            android:noHistory="true"></activity>


        <!-- COMMENT LIST -->
        <activity
            android:name=".vue.activity.CommentLogActivity"
            android:label="@string/commentLog_title"
            android:launchMode="singleInstance"></activity>
        <!-- COMMENT -->
        <activity
            android:name=".vue.activity.edit.EditCommentActivity"
            android:label="@string/editComment_title"
            android:noHistory="true"></activity>

        <!-- Shopping item -->
        <activity
            android:name=".vue.activity.edit.EditShoppingItemActivity"
            android:label="@string/createShoppingItem_title"
            android:noHistory="true"></activity>

    </application>

</manifest>
